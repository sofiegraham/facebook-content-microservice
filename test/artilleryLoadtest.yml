config:
  target: 'http://localhost:9000'
  phases:
    - duration: 1 # ramp up time
      arrivalRate: 10 # amount of users to ramp up per second
  defaults:
    headers:
      x-my-service-auth: '987401838271002188298567'
scenarios:
  - flow:
    - get:
        url: "/users/3/feed"
    # - get:
    #     url: "/users/5000/page_likes"
    # - post:
    #       url: "/users/5/posts"
    #       json:
    #         content: "This is a string which must be written to the database!"
